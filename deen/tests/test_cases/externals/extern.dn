extern "C" fn malloc(usize) *void;
extern "C" fn free(*void);

fn main() i32 {
  let int_ptr: *i32 = malloc(sizeof!(i32));

  *int_ptr = 100;

  println!("{}", *int_ptr);

  *int_ptr = 123;

  println!("{}", *int_ptr);

  free(int_ptr);

  return 0;
}
